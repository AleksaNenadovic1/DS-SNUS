apiVersion: v1
kind: Service

metadata:
  name: notification
  namespace: ds-snus

spec:
  selector:
    com.docker.compose.project: ds-snus
    com.docker.compose.service: notification

  ports:
  - name: http
    port: 8080
    targetPort: 8080