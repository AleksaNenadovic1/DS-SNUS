apiVersion: v1
kind: Service

metadata:
  name: ingress
  namespace: ds-snus

spec:
  type: NodePort

  selector:
    com.docker.compose.project: ds-snus
    com.docker.compose.service: ingress

  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 30080